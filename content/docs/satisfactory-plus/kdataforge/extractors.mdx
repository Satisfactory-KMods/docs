---
title: "Adding new Extractors / Remove Extractors"
description: How to add new Extractor or Remove Extractor DataAssets to Satisfactory Plus through KAPI. To allow / disallow them in SF+ gameplay.
sidebar:
  order: 1
---

## The DataAsset

For adding Extractors or Remove Extractors to Satisfactory Plus, you need to create a new DataAsset of type `KAPIExtractorAllowList` the header can be found [HERE](https://github.com/Satisfactory-KMods/public-source/blob/main/KAPI/Source/KAPI/Public/DataAssets/KAPIExtractorAllowList.h).

## Properties

| Property              | Type                                                     | Description                                                                                       |
| --------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `mAllowedExtractors`  | `TArray<TSubclassOf<AFGBuildableResourceExtractorBase>>` | The list of extractor classes that are allowed.                                                   |
| `mDisallowExtractors` | `TArray<TSubclassOf<AFGBuildableResourceExtractorBase>>` | The list of extractor classes that are disallowed.                                                |
| `mAllowedResources`   | `TArray<TSubclassOf<UFGResourceDescriptor>>`             | The list of resource classes that are allowed.                                                    |
| `mDisallowResources`  | `TArray<TSubclassOf<UFGResourceDescriptor>>`             | The list of resource classes that are disallowed.                                                 |
| `mPriority`           | `int32`                                                  | The priority of this DataAsset. Higher priority means it will override lower priority DataAssets. |

## Notes

- There is a priority system for the DataAssets. If a other mod disallows an extractor or resource, it will be disallowed even if Satisfactory Plus allows it. This is to prevent conflicts between mods.
  - These priorities can be managed by the `mPriority` property in the DataAsset. The higher the priority, the more weight it has. The default priority is 0.
- Creating a new `UFGResourceDescriptor` for a brand-new resource type is out of KAPI's scope `mAllowedResources`/`mDisallowResources` only reference existing resource descriptor classes.

See [Adding KAPI Data Assets](/kdataforge/examples/kapi-data-assets/) for every KAPI type's
`type: dataasset` example in one place.
