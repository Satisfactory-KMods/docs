---
title: Canvas
description: Placing nodes, wiring ports and keeping a large model readable.
sidebar:
  order: 2
---

The canvas is where the model lives. Pan with the left mouse button, zoom with
the wheel around the cursor, and drag with the right button to pull a selection
box. Shift or Control adds to a selection instead of replacing it.

A selected node is outlined, and every connection running into or out of it
lights up with it. Select several nodes and the whole set lights up together, so
a path through a large model can be followed without clicking each connection.
A broken connection keeps its own colour, because that matters more than the
path it belongs to.

Right click a node for what you can do with it: open a container, edit it in the
inspector, select the whole chain it belongs to or every node like it, duplicate
or copy it, disconnect everything, or delete it. Right clicking empty canvas is
what opens the palette, and so is double clicking it. Double clicking a node
puts the cursor in its inspector instead, and double clicking a container opens
it.

Selecting a chain and then arranging it lays it out along its own connections,
left to right, without moving the block somewhere else on the canvas. Arrange
sits in the action bar and on the L key. Fitting the view fits the selection
when there is one, and the whole container when there is not.

The arrow keys move whatever is selected. They step by the building grid while
it is on, so a nudged node lands where a dragged one would, and Shift covers
more ground. A whole run of presses is one step in the history.

## The panel beside the canvas

The right hand panel carries three tabs. The summary totals the model, the
inspector edits whatever is selected, and messages lists everything the solver
had to say. Click a node and the panel switches to the inspector on its own, so
a selection and its settings are never more than one click apart.

Messages name the node they are about rather than an internal identifier, and
clicking one selects that node on the canvas. The same complaint about forty
nodes is one row with a count beside it, so a single unfed port cannot bury
everything else, and the whole list can be copied as text for asking about it.
Only a solve that runs long enough to be worth stopping still interrupts the
canvas itself.

The summary totals more than rates. It says which belts or pipes the model
needs and how full they run, which unlocks it depends on, and which mods it
draws on, and it can be copied as text the same way. A building whose limit the
model exceeds is reported among the messages rather than only in a panel row.

## Connections

Drag from a port to a compatible port to connect them. A port can carry several
connections, and their order matters: when a split has to prioritize, the solver
follows the order you see. Reorder them from the inspector.

Drop a connection on empty space and the palette opens filtered to that port, so
the node you pick arrives already wired.

Each port can carry a limit, and the inspector says which of the two it is: at
most that rate, or exactly that rate. A limited port carries a small badge on
the canvas, so a port pinned to a rate never looks like a free one. A limit
applies to whatever building carries the port, a Boiler or a Turbine as much as
a recipe, and a limit on a port nothing carries away sizes the node itself.

A selection copied on the canvas travels through the system clipboard, so it can
be pasted into another model or another tab.

## Waypoints

A connection can be routed through as many waypoints as you like. Double click a
connection to add one, double right click a waypoint to remove it, and double
right click a connection to delete it. Waypoints follow when you move both ends
of a connection together.

Three path styles are available: curves, direct lines, and orthogonal routing.

## Grids

The canvas sits on the same background grid as the rest of the site. It is
decoration, not a snap target.

Two working grids exist on top of it and they are separate. The building grid snaps nodes, the
connection grid snaps waypoints, and each has its own X and Y spacing. Both can
be applied to everything, to the current outpost, or to the current selection.

## Outposts and blueprints

An outpost holds part of a model. Enter it to work inside, double click it to
open it, and press Escape to come back out. A closed one says how much it holds,
so a folder on the canvas does not have to be opened to be read. Each one keeps its own pan and zoom, so returning to an outpost
puts you back where you left. Select All only takes the outpost you are in.

A blueprint scales its contents by a count, which is how you model the same
sub factory many times over without drawing it many times over.

## Aligning a port

A port with a connection can be aligned to it. The node moves vertically until
the port sits on the same line as the port it feeds, and the horizontal layout
you built stays untouched. With several connections the first one wins.
